# myarm_m750_msgs

Dedicated ROS 2 interface package. Keep interfaces framework-neutral where a
standard ROS message already exists; add custom messages only when the contract
cannot be represented by standard messages.
