# Camera calibration

Đặt calibration đã chứng nhận theo hardware identity, không theo role. Loader
v0.2 kiểm tra kích thước ma trận, giá trị finite và kích thước ảnh so với stream.
`mock_640x480.yaml` chỉ dành cho backend mock, không phải calibration phần cứng.
